REGION_ALIAS("TEXT_STORE", PSRAM);
REGION_ALIAS("RAM_STORE", PSRAM);
REGION_ALIAS("PSRAM_STORE", PSRAM);
REGION_ALIAS("XIP_RAM_STORE", XIP_RAM);
REGION_ALIAS("SCRATCH_X_STORE", SCRATCH_X);
REGION_ALIAS("SCRATCH_Y_STORE", SCRATCH_Y);
